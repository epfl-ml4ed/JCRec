#!/bin/bash
#SBATCH --job-name=a2c_proba_k_1_cpu_gpu
#SBATCH --nodes=1
#SBATCH --ntasks=1
#SBATCH --cpus-per-task=4
#SBATCH --time=5:00:00
#SBATCH --gres=gpu:0     # je demande 5 GPU au total, un pour chaque tâche

# on navigue vers le répertoire contenant le script bash
#cd "$(dirname "$0")"

# On exécute le script a2c_run.sh 
bash entrypoint.sh a2c_run.sh
